package org.vitalii.fedyk.sqs.usecase;

/** Use case interface for processing messages. */
public interface MessageProcessorUseCase {
  /** Executes the message processing use case. */
  void execute();
}
